#include <stdio.h>
#include <stdlib.h>


typedef struct Node{
    int value; 
    int * next;
}Node; 

int * createNode (int value){
    Node * newnode = malloc(sizeof(Node));
    
}
int main(void){
    

}